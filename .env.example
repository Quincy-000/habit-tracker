# Copy to .env to override defaults. Everything below is already the
# default when unset — the repo contains no hardcoded credentials.
POSTGRES_USER=habits
POSTGRES_PASSWORD=habits
POSTGRES_DB=habits
